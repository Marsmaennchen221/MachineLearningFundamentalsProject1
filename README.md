# To Build
Just run the makefile, will put the executable in bin/ann.x
```bash
make build
```

# To Run
Run with the data file you wish to train and test on.
Will print out some statistics and dump a .csv of loss statistics across epochs of a random run.
```bash
bin/ann.x [data_file.csv]
```
